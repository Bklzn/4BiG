class Delivery{
    constructor(PackageNum, PackedDate, SendDate, RecivedDate){
        this.PackageNum = PackageNum
        this.PackedDate = PackedDate
        this.SendDate = SendDate
        this.RecivedDate = RecivedDate
    }
}